# OpenMV Cam H7 改造成 Windows 原生摄像头：开发与调试报告

日期：2026 年 9 月 23 日；硬件：OpenMV Cam H7，OpenMV 构建目标 `OPENMV4`。

上游基线：[openmv/openmv v4.7.0](https://github.com/openmv/openmv/tree/v4.7.0)，提交 `2206dcb31c2a854c79e83cd62d6b55939f6c351a`。

## 1. 我想解决的问题，以及我做了什么

我希望把手头直接连接到 Windows 电脑的 OpenMV 变成真正的系统摄像头，让普通会议、录像和测试软件直接选用它，而不是让 OpenMV IDE 预览画面，再通过 OBS 等软件做虚拟摄像头。项目开始后，我把需求进一步收紧为：优先保障真实画面、Windows 原生识别和稳定性，然后在 **30 fps 条件下尽量提高原生分辨率**。对于 YUY2 与 MJPEG 的选择，我主动追问了传输上限，最后决定切换到 MJPEG 路线。

这不是一个只看编译是否通过的软件实验。我负责给出目标和取舍、在同一 USB 接口上反复配合设备重插与刷写、观察电脑端的真实使用效果、指出画面上下颠倒的问题，并根据每轮实测决定下一步是否值得继续。源码修改、交叉编译、自动化抓帧和数据整理过程中，我使用了 AI 编程助手协助；本报告不把自动生成的代码冒称为我逐行手写，也不把我在需求决策和实机验证中的工作抹去。

最终结果是一个可被 Windows 自带 USB 视频驱动识别的 MJPEG UVC 摄像头。当前版本已修正上下方向；在本机的 320×240 档，连续采集 600 帧的平均值为 43.00 fps。更大尺寸能够成像，但 352×288 以上的实测只有约 21～22 fps，因此 **320×240 是本次验证达到至少 30 fps 的最高原生尺寸**。

## 2. 设备识别与前期风险

最初通过 Windows USB 枚举与 OpenMV IDE 信息确认设备是 OpenMV Cam H7，对应固件目标 `OPENMV4`，而不是 H7 Plus。普通 OpenMV 固件时期出现过 `37c5:1204`；OpenMV DFU 引导器使用 `37c5:9204`；STM32 ROM DFU 可出现 `0483:df11`。这些标识用于区分运行、刷写和恢复状态，不能把不同分区或不同板型的二进制混刷。

前期曾找到旧版 OpenMV IDE 资源包里的 `OPENMV4/uvc.bin`，但“名称相同”没有证明它兼容当前板卡与引导器。一次旧固件尝试后，设备曾不能正常枚举，随后通过 STM32 ROM DFU/引导器恢复路径继续工作。这次教训直接影响了后续做法：保留可回退固件，核对 VID/PID 与刷写分区，不把第三方现成固件“能下载”当作“能在这块板上安全运行”。本仓库不上传恢复阶段的整片 Flash 备份，因为其中可能包含设备特有数据。

## 3. 技术方案与理论约束

Windows 原生摄像头采用 UVC 协议；电脑端直接使用系统驱动 `usbvideo.sys`。这块板在本次配置中是 USB Full Speed，不是 High Speed。视频接口采用等时传输，每个 USB 毫秒帧的一次有效负载最多按当前描述符中的 1022 字节包计算，其中 2 字节是 UVC 负载头，约有 1020 字节留给图像。因此当前实现的 USB 图像净载荷上界约为 **1.02 MB/s**，这是描述符/时隙估算，不是实测吞吐保证。

YUY2 每像素需要 2 字节。320×240×2×30 = 4.608 MB/s，远超上述链路预算；仅继续缩短 USB 发送代码无法让这个尺寸的原始 YUY2 达到 30 fps。MJPEG 改为每帧独立 JPEG 压缩，典型帧长度随场景变化，使 320×240/30 fps 在 USB 带宽上成为可能。按 1.02 MB/s 粗算，30 fps 平均每帧须控制在约 34 kB 以下；实际还要给协议开销、调度波动留余量。

这套固件在 OpenMV 端采集 YUV422，调用硬件 JPEG 编码路径，按 UVC 帧头分包发送。JPEG 质量参数目前是 45；最大帧缓冲 64 KiB。USB 包末尾设置帧结束标记并切换 FID 位，让 Windows 正确区分连续帧。对于 320×240 及以下尺寸保留两个 CSI 帧缓冲；更大尺寸只保留一个，避免占满可用 framebuffer。这个内存策略的代价是大尺寸难以用双缓冲掩盖采集、编码和发送之间的等待。

## 4. 开发与调试经过

### 4.1 让设备先作为 UVC 摄像头出现

上游 v4.7.0 仍保留 STM32 UVC 示例源码，但缺少当前工程直接可用的独立顶层构建目标。我先补上 `make ... uvc` 的目标和链接所需对象，再修正 USB 描述符、视频格式协商和发送循环。早期版本用测试图案而不依赖传感器，目的是把“USB 视频接口是否能送出帧”与“摄像头采集/DMA 是否正确”分开验证；之后才接入实际传感器画面。Windows 最终将设备识别为 `OpenMV UVC in FS Mode`，归类为摄像头，使用微软驱动。

测试中碰到过“设备能枚举，但画面不是可用实拍”的阶段。测试图案证明了 USB 侧可以工作，却不代表相机链路也完成了；后续检查采集回调、DMA/帧缓冲状态和像素排列，直到抓取到真实画面。颜色问题又单独修正，不能只因为画面有内容就宣称颜色正确。

### 4.2 从 YUY2 转向 MJPEG

我先要求确认当前协议在 30 fps 下的上限，随后询问 MJPEG 是否比 YUY2 更合适，并明确选择 MJPEG。改造包括：增加 MJPEG 格式与帧描述符、只接受正确的 MJPEG 协商参数、初始化硬件 JPEG、将图像压缩后的实际长度（而不是固定的原始帧长度）装入 UVC 数据包。首个稳定版提供 160×120、240×160、320×240；在 Windows 端确认列出的确是 MJPEG，而不仅是设备名称变了。

期间抓取过原始 JPEG，检查帧头 `FFD8` 与帧尾 `FFD9`，并用 FFmpeg 连续解码。一次初始抓帧出现过缺少 EOI 的提示，随后连续采集没有重现；它被记录为可能的启动瞬间不完整帧，而不是被当作已证实的持续性故障。只看单帧截图不足以判断帧率，所以又编写脚本按指定帧数计时，输出 `frames / elapsed_s`。

帧率脚本还踩过一个典型测量陷阱：如果 FFmpeg 进度输出被重定向却不并行读取，管道可能填满，使进程阻塞、测试超时，看起来像相机掉帧。修正为在 FFmpeg 运行时异步排空标准输出和错误输出，再计算完整采集耗时。报告中的数值采用这一修正后的测试方法。

### 4.3 扩展分辨率与时钟实验

在 320×240 已经达到目标帧率后，我要求继续探索 30 fps 的更高分辨率。固件增加了 352×288、400×300、480×320 三档。三档都能被 Windows 选择且可以解码真实画面，但实际平均速度降到约 21 fps。320×240 与 352×288 之间出现明显速度分界；代码检查显示 OV7725 的驱动会在宽高超过 320×240 时转入 VGA 读出路径。不过，本次没有用独立的传感器身份读取结果证明当前模组一定是 OV7725，因此“VGA 路径造成分界”是有源码依据的解释，不是已定位完成的唯一根因。

为了验证是否受 OV7725 的像素时钟限制，做过一个只对该传感器生效的 PLL 试验；其他传感器不执行调速。刷入后，320×240 得到 37.21 fps，352×288 为 20.97 fps，400×300 为 20.83 fps，480×320 为 21.01 fps，高尺寸没有改善。于是撤销实验代码，并把设备刷回试验前的固件；重新编译文件与备份的 SHA-256 完全一致。回归测试中，320×240 的 600 帧平均 42.54 fps，而 352×288 的 600 帧平均 22.31 fps。这个结果支持“单纯提高该 PLL 没有效果”，但还不足以把所有可能瓶颈都归因于传感器。后续若继续优化，需要分别测量采集、JPEG 编码和 USB 发送耗时，而不是盲目调时钟。

### 4.4 上下颠倒的用户验收问题

我在实际使用画面时发现上下方向颠倒，并明确反馈。固件随后在每次切换分辨率后调用传感器的垂直翻转接口 `omv_csi_set_vflip(1)`；OV7725 和 MT9M114 驱动都实现了该接口。这是传感器端翻转，没有把 JPEG 解码后再由电脑软件翻转，因此仍是普通应用可直接使用的 UVC 画面。

修正版刷入后，Windows 端重新抓到正立的实拍画面。最终再测 320×240：600 帧用时 13.952 秒，平均 **43.00 fps**。实拍验证图只留在本地，没有公开上传，避免把人的面部或房间环境放到公开仓库。

## 5. 故障、原因与处理对照

| 现象 | 诊断结论 / 注意点 | 处理与验证 |
| --- | --- | --- |
| 旧 UVC 固件刷后设备不正常枚举 | 旧文件名中的 `OPENMV4` 不能证明对当前板卡/引导器兼容 | 使用 ROM DFU/引导器恢复路径，改为当前源码编译并保留回退固件 |
| UVC 设备出现但没有可用真实画面 | USB 传输、传感器采集和 DMA 是不同环节 | 先测试图案，再接入采集，检查缓冲/中断，实拍抓帧验证 |
| 画面颜色不正确 | 像素排列与 UVC 格式不一致 | 修正颜色处理，实际抓图确认 |
| YUY2 分辨率无法在 30 fps 继续提高 | 320×240/30 的原始数据量已超过 FS 链路预算 | 由我决定改用 MJPEG，硬件 JPEG 压缩后再 UVC 分包 |
| FFmpeg 长时间测试疑似卡住 | 进度输出的管道未并行排空可造成假性阻塞 | 脚本异步读取两条输出流；只统计完整采集 |
| 352×288 以上只有约 21～22 fps | 当前整条采集→编码→传输链路未达到 30 fps；具体单点瓶颈未完全测定 | 做条件化 PLL 实验，无效后撤回，不虚报 30 fps |
| 画面上下颠倒 | 摄像头安装方向与传感器默认输出方向不一致 | 传感器端启用垂直翻转，抓帧和帧率回归通过 |
| 摄像头在受限命令环境中“能列出却打不开” | 设备存在，但受限进程不能绑定 DirectShow 设备；这不是固件再次损坏 | 在具有设备访问权限的宿主环境重测，成功抓帧与计时 |

## 6. 可复核的数据、文件与局限

最初稳定发布文件（v1.0.0）：`firmware/openmv-h7-uvc-mjpeg-vflip.bin`，119,484 字节，SHA-256：

```text
0699F7FCE193E85A316783D3F1124A74A78B486ADCB850C35D798A6B51ED2FFD
```

固件同时暴露 160×120、240×160、320×240、352×288、400×300、480×320 六个 MJPEG 档位。表中的速度是 Windows/FFmpeg 收到帧的结果，非标称 USB 帧间隔。当前描述符对较大分辨率仍声明 30 fps，而设备实测无法达到；这是一项应在后续版本中修正的契约不一致。应用程序若需要稳定 ≥30 fps，应明确选择 320×240。600 帧测试是当前本机连续采集验证，不等于长时间、跨主机和跨场景的稳定性认证。

进一步提高真实 30 fps 分辨率，优先工作应是：在设备端分别计时传感器采集、JPEG 编码、UVC 发送和等待；测量各档 JPEG 长度分布；确认实际传感器型号及其帧时序；评估大尺寸单缓冲带来的等待；再决定是优化流水线、降低 JPEG 质量还是更换高速 USB 硬件。无论采用哪种策略，都要保持“真实分辨率”和“真实帧率”一致，不能靠把 320×240 插值成更大画面宣称突破。

## 7. 项目总结

我把“让 OpenMV 在 Windows 上成为可选摄像头”的想法，逐步收敛为可检验的协议、画质、帧率和方向要求；在风险较高的刷写阶段反复配合实机操作，并用实际画面指出问题。最终用 MJPEG 和 UVC 固件解决了系统识别与带宽限制，在 320×240 达到目标帧率，同时诚实保留了高分辨率未达 30 fps 的结果。这份仓库把固件、源码补丁和测试方法一起公开，方便别人复现、审查和继续改进。

## 8. 后续修订：左右镜像与 Windows Hello 可行性

我在完成上下翻转后又提出左右镜像要求，并询问能否进一步用于 Windows Hello 人脸登录。左右镜像没有改动电脑端软件，而是在分辨率配置后增加 `omv_csi_set_hmirror(1)`，与原有的 `omv_csi_set_vflip(1)` 并用。第一次重新插拔时刷写工具未捕获短暂的 OpenMV DFU 状态，Windows 仍枚举旧 UVC 固件；改用此前成功的刷写运行方式并再次重插后，DFU 记录显示 119,572 字节全部写入。启动后的首帧一度很暗，随后确认镜头前存在遮挡；移开遮挡后能连续抓取实拍画面。320×240 连续 600 帧用时 13.946 秒，平均 43.02 fps，未见帧率退化。

最新版 v1.1.0：`firmware/openmv-h7-uvc-mjpeg-vflip-hmirror.bin`，119,572 字节，SHA-256：

```text
839498DE1DCE4FA3FBC2AB34ED2602A3B647B2F70EB8C04DEB2147152B609F76
```

对 Windows Hello，我没有把“已有普通 UVC 视频”误当作“已有生物识别摄像头”。[微软的人脸认证文档](https://learn.microsoft.com/en-us/windows-hardware/design/device-experiences/windows-hello-face-authentication)要求专门配置的近红外成像；其 [UVC 实现指南](https://learn.microsoft.com/en-us/windows-hardware/drivers/stream/uvc-camera-implementation-guide)要求相应的 RGB/IR 视频类型和 Face Auth Profile V2。OpenMV H7 虽有红外补光 LED，但当前固件只有一条彩色 MJPEG 流，没有独立 IR 流，也没有满足 Face Auth Profile 的已验证硬件能力。因此 v1.1.0 不支持 Windows Hello；若要研究该方向，需要先确认或改造红外成像硬件，再实现独立视频流、描述符与认证验证，而不是简单更改设备标识。当前项目没有做这类安全敏感的验证。
